2.用集合存储三个猫类对象，并将这三个猫类对象的属性，
   使用两种种方法遍历输出（Iterator、for-each）
   
   
   2、将上题改进利用Math.random（）产生这两个集合中的元素，然后对他们实施“交”“并”操作。
   
   3、创建五个学生对象（姓名、语文成绩、数学成绩、英语成绩）
存入set集合

4、按照总分的从高到低将学生的姓名和总分输出到控制台